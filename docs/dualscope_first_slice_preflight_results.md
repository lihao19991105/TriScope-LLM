# DualScope First Slice Preflight Results

The generated preflight outputs live in:

- `outputs/dualscope_minimal_first_slice_real_run_preflight/default`
- `outputs/dualscope_minimal_first_slice_real_run_preflight_analysis/default`

The result should be interpreted as an environment and contract readiness check, not a real experiment.

