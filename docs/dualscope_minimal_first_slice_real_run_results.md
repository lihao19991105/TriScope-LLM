# DualScope Minimal First-Slice Real-Run Results

Results are stored under `outputs/dualscope_minimal_first_slice_real_run/default`.

Current expected interpretation:

- Artifact chain evidence: valid if required artifacts and contracts pass.
- Performance evidence: not yet available if labels are missing or metric placeholders are used.
- Model-inference evidence: not available if Stage 1 / Stage 2 summaries report protocol-compatible deterministic mode.

