# DualScope First Slice Artifact Validator

This validator defines the required Stage 1, Stage 2, Stage 3, evaluation, report, verdict, and recommendation artifacts for future real-run outputs.

The hardening stage only creates validator rules. It does not run the real first slice.
