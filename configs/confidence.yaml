# Minimal confidence probing configuration for TriScope-LLM.

default:
  confidence_template_name: "minimal_v1"
  query_budget: 8
  candidate_query_source: "poisoned_only"
  generation:
    max_new_tokens: 24
    top_k_capture: 5
    do_sample: false
    temperature: 0.0

smoke:
  confidence_template_name: "minimal_v1"
  query_budget: 2
  candidate_query_source: "poisoned_only"
  generation:
    max_new_tokens: 16
    top_k_capture: 5
    do_sample: false
    temperature: 0.0

pilot_local:
  confidence_template_name: "minimal_v1"
  query_budget: 3
  candidate_query_source: "poisoned_only"
  generation:
    max_new_tokens: 12
    top_k_capture: 5
    do_sample: false
    temperature: 0.0
