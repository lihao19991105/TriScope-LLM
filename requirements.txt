# TriScope-LLM bootstrap requirements
#
# Notes:
# - `torch` is intentionally not pinned here because CUDA-specific installation
#   depends on the local environment. Install a suitable PyTorch build first.
# - `xgboost` is deferred for now. It is planned for later fusion baselines,
#   but it is not required to complete the current bootstrap milestones.

# Model and training stack
transformers>=4.40,<5.0
protobuf>=4.25,<8.0
hf_transfer>=0.1.9,<0.2
peft>=0.10,<1.0
accelerate>=0.30,<1.0
bitsandbytes>=0.43,<0.47
datasets>=2.18,<3.0

# Configuration and utility
PyYAML>=6.0,<7.0
tqdm>=4.66,<5.0

# Analysis and evaluation
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0
matplotlib>=3.8,<4.0

# Deferred for a later fusion milestone:
# xgboost>=2.0,<3.0
